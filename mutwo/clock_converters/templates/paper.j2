

{% if print_page_number %}

    oddHeaderMarkup = \markup \fill-line { " " }
    evenHeaderMarkup = \markup \fill-line { " " }
    oddFooterMarkup = \markup {
      \fill-line {
        \typewriter \fontsize #-1
        \fromproperty #'page:page-number-string
      }
    }
    evenFooterMarkup = \markup {
      \fill-line {
        \typewriter \fontsize #-1
        \fromproperty #'page:page-number-string
      }
    }

    print-first-page-number = ##t

{% else %}

    print-page-number = ##f

{% endif %}


{% if top_margin is not none %}
top-margin = {{ top_margin }}\cm
{% endif %}

{% if bottom_margin is not none %}
bottom-margin = {{ bottom_margin }}\cm
{% endif %}

{% if left_margin is not none %}
left-margin = {{ left_margin }}\cm
{% endif %}


{% if foot_separation is not none %}
foot-separation = {{ foot_separation }}\cm
{% endif %}

{% if head_separation is not none %}
head-separation = {{ head_separation }}\cm
{% endif %}


{% if line_width is not none %}
line-width = {{ line_width }}\cm
{% endif %}


{% if before_title_space is not none %}
before-title-space = {{ before_title_space }}\cm
{% endif %}

{% if after_title_space is not none %}
after-title-space = {{ after_title_space }}\cm
{% endif %}

{% if between_title_space is not none %}
between-title-space = {{ between_title_space }}\cm
{% endif %}


{% if page_top_space is not none %}
page-top-space = {{ page_top_space }}\cm
{% endif %}


{% if page_breaking is not none %}
page-breaking = {{ page_breaking }}
{% endif %}


% Deactivated
% system-separator-markup = \markup \fill-line { \override #'(span-factor . 1/16) \draw-hline }

system-system-spacing = #'(
    (basic-distance . {{ system_system_basic_distance }})
    (padding . {{ system_system_padding }})
)
score-system-spacing = #'(
    (basic-distance . {{ score_system_basic_distance }})
    (padding . {{ score_system_basic_distance }})
)
markup-system-spacing = #'(
    (basic-distance . {{ markup_system_basic_distance }})
    (padding . {{ markup_system_basic_distance }})
)

#(define fonts
    (make-pango-font-tree "{{ font }}" "{{ font }}" "{{ font }}"
    (/ staff-height pt {{ staff_height }}))
)

#(define fonts
  (set-global-fonts
    #:music "beethoven"
    #:brace "beethoven"
  )
)
